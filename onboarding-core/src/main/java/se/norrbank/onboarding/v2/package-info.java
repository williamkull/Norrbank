/**
 * The v2 REST surface. New endpoints go here.
 *
 * <p>Migration from v1 is endpoint by endpoint as clients move over; the two run side by
 * side until the last v1 consumer is gone.
 */
package se.norrbank.onboarding.v2;
