package se.norrbank.screening.rules;

/** The lists a party is screened against on each run. */
public enum ScreeningList {
    EU_CONSOLIDATED_SANCTIONS,
    OFAC_SDN,
    UN_CONSOLIDATED,
    PEP_GLOBAL,
    ADVERSE_MEDIA
}
