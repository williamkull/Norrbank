# screening-batch runtime configuration.
# Overridden per environment by -D system properties from the wrapper script.

stage.file.path=/var/lib/norrbank/screening/case-stage.dat

# A run holds the stage file lock for its whole duration. Operations schedules every
# reader of the file outside this window.
batch.expected.runtime.minutes=4

db.url=jdbc:postgresql://onboarding-db.norrbank.internal:5432/onboarding
db.user=screening_batch
